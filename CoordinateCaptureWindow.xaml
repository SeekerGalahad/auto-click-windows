<Window x:Class="AutoClickTool.CoordinateCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoClickTool"
        mc:Ignorable="d"
        Title="坐标捕获工具" Height="150" Width="300" Topmost="True" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="将鼠标移动到目标位置，按F2捕获坐标" Margin="10,10,10,5" TextWrapping="Wrap" TextAlignment="Center"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
            <TextBlock Text="X:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox x:Name="XCoordinateTextBox" Width="50" IsReadOnly="True" VerticalContentAlignment="Center" Margin="5,0"/>
            
            <TextBlock Text="Y:" VerticalAlignment="Center" Margin="15,0,5,0"/>
            <TextBox x:Name="YCoordinateTextBox" Width="50" IsReadOnly="True" VerticalContentAlignment="Center" Margin="5,0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="CaptureButton" Content="捕获(F2)" Width="80" Margin="5,0" Click="CaptureButton_Click"/>
            <Button x:Name="ConfirmButton" Content="确认" Width="80" Margin="5,0" Click="ConfirmButton_Click"/>
            <Button x:Name="CancelButton" Content="取消" Width="80" Margin="5,0" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>