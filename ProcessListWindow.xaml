<Window x:Class="AutoClickTool.ProcessListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoClickTool"
        mc:Ignorable="d"
        Title="选择进程窗口" Height="500" Width="700" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="SearchTextBox" Width="200" Margin="0,0,10,0" TextChanged="SearchTextBox_TextChanged"/>
            <Button x:Name="RefreshButton" Content="刷新列表" Padding="10,3" Click="RefreshButton_Click"/>
        </StackPanel>
        
        <ListView Grid.Row="1" x:Name="ProcessListView" Margin="0,0,0,10" SelectionChanged="ProcessListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="进程ID" DisplayMemberBinding="{Binding ProcessId}" Width="60"/>
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding ProcessName}" Width="150"/>
                    <GridViewColumn Header="窗口标题" DisplayMemberBinding="{Binding Title}" Width="310"/>
                    <GridViewColumn Header="类名" DisplayMemberBinding="{Binding ClassName}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SelectButton" Content="选择" Width="80" Margin="5,0" Padding="10,3" Click="SelectButton_Click" IsEnabled="False"/>
            <Button x:Name="CancelButton" Content="取消" Width="80" Margin="5,0" Padding="10,3" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 